# Environment variables for Allora-Hyperliquid Trade Assistant

# Allora API Configuration
# Get your API key from the Allora team/Discord: https://discord.com/invite/allora
# Ensure this key is kept secret and never committed to version control in .env.local
ALLORA_API_KEY=YOUR_ALLORA_API_KEY
# Optional: Specify the Allora API endpoint if different from default
# ALLORA_API_ENDPOINT=https://api.allora.network/v2

# Hyperliquid API Configuration
# Generate API keys on Hyperliquid with Read and Trade permissions.
# IMPORTANT: Store the private key (starting with 0x...) as the SECRET.
# Keep these keys secret and never committed to version control in .env.local
HYPERLIQUID_API_KEY=YOUR_HYPERLIQUID_READ_API_KEY
HYPERLIQUID_API_SECRET=YOUR_HYPERLIQUID_TRADE_PRIVATE_KEY

# Database Configuration (Using SQLite for local development)
# This URL points to the local SQLite database file.
DATABASE_URL="file:./db/dev.db"

# Optional: Set to "true" to use Hyperliquid Testnet instead of Mainnet
# HYPERLIQUID_USE_TESTNET=false
